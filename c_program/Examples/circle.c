#include<stdio.h>
void main()
{
    float r;
    float pi=3.148;
    float area;
    printf("Enter the rdius of the circle:");
    scanf("%f",& r);
    area=pi*r*r;
    printf("%d", area);
}