#include<stdio.h>
int main()
{
    int year;
    printf("Enter the year");
    scanf("%d",year);
    if(year%100)
    {
        printf("%d is a leap year",year);        
    }
    else
    {
        if(year%4)
        {
            printf("%d is greatest",c);
        }
        else
            printf("%d is greatest",c);
    }
    return 0;
}